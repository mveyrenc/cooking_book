<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title><%= I18n.t 'application.name' %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= action_cable_meta_tag %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <meta name="description" content="">
  <meta name="author" content="">
  <%= stylesheet_link_tag 'devise',
                          media: 'all',
                          'data-turbo-track': 'reload' %>
  <%= stylesheet_link_tag 'print',
                          media: 'print',
                          'data-turbo-track': 'reload' %>
  <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
  <!--[if lt IE 9]>
  <%= javascript_include_tag "https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js",
                             "https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js" %>
  <![endif]-->
  <%= javascript_pack_tag "application",
                          'data-turbo-track': 'reload' %>
</head>
<body>

<section class="hero is-primary is-fullheight">
  <div class="hero-body">
    <div class="container">
      <% flash.each do |name, msg| %>
        <%= render(Application::Elements::NotificationComponent.new(
            color: name,
            delete: true
        )) do %>
          <%= msg %>
        <% end unless msg.blank? %>
      <% end %>
      <div class="columns is-centered">
        <div class="column is-5-tablet box">
          <%= yield %>
        </div>
      </div>
    </div>
  </div>
</section>

<footer>
</footer>
</body>
</html>