<section class="section">
  <div class="container box">
    <div class="title is-size-1"><%= I18n.t( 'recipes.new_page_title' ) %></div>
    <%= render(Recipes::Form.new(item: @recipe)) %>
  </div>
</section>
