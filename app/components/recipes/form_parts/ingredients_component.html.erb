<%= form.text_field :quantity %>
<%= form.markdown :ingredients %>
<%= form.collection_select(
  :category_ids,
  Categorization::COOKING_MAIN_INGREDIENT.categories,
  :id,
  :to_s,
  {},
  { multiple: true }
) if object.book == Categorization::COOKING_MAIN_INGREDIENT.book %>