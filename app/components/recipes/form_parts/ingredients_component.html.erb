<%= form.text_field :quantity %>
<%= form.markdown :ingredients %>
<%= form.collection_select(
  :category_ids,
  Category.cooking_main_ingredient,
  :id,
  :to_s,
  {},
  { multiple: true }
) if object.book == Book.COOKING %>