<% if objects.blank? %>
  <%= t('application.no_entry') %>
<% else %>
  <div class="container is-widescreen">
    <div class="columns is-multiline">
      <% objects.each do |item| %>
        <%= render(Users::ListItemComponent.new(object: item)) %>
      <% end %>
    </div>
  </div>
<% end %>
