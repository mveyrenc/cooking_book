<% if user_signed_in? %>
  <nav class="navbar is-primary is-fixed-top" role="navigation" aria-label="main navigation">
    <div class="container">
      <%= render(MainMenu::Brand) %>
      <div id="main-navbar-menu" class="navbar-menu">
        <%= render(MainMenu::MainNavbar) %>
        <%= render(MainMenu::UserNavbar) %>
      </div>
    </div>
  </nav>
<% end %>