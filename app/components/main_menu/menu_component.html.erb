<% if user_signed_in? %>
  <nav class="navbar is-light-gray is-fixed-top" role="navigation" aria-label="main navigation">
    <div class="container">
      <%= render(MainMenu::BrandComponent.new) %>
      <div id="main-navbar-menu" class="navbar-menu">
        <div class="navbar-start">
          <%= render(MainMenu::BooksComponent.new) %>
        </div>
        <div class="navbar-end">
          <%= render(MainMenu::AdminNavbarComponent.new) %>
          <%= render(MainMenu::UserNavbarComponent.new) %>
        </div>
      </div>
    </div>
  </nav>
<% end %>