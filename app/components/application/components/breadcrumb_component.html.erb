<% if breadcrumb_trail.any? %>
  <nav class="<%= css_class %>">
    <ul>
      <% breadcrumb_trail.each do |crumb| %>
        <li class="<%= 'is-active' if crumb.current? %>">
          <%= link_to crumb.name, crumb.url, (crumb.current? ? { 'aria-current': 'page' } : {}) %>
        </li>
      <% end %>
    </ul>
  </nav>
<% end %>