<%= render(Bulma::Form::Entry,
           form: form,
           method: method.to_sym,
           resource: resource,
           options: options,
           label_html_options: label_html_options
    ) do %>
  <%= render(Bulma::Form::Email::Field,
             form: form,
             method: method.to_sym,
             resource: resource,
             options: options,
             html_options: field_html_options
      ) %>
<% end %>