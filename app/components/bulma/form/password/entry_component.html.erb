<%= render(Bulma::Form::EntryComponent,
           form: form,
           method: method.to_sym,
           resource: resource,
           options: options,
           label_html_options: label_html_options
    ) do %>
  <%= render(Bulma::Form::Password::FieldComponent,
             form: form,
             method: method.to_sym,
             resource: resource,
             options: options,
             html_options: field_html_options
      ) %>
<% end %>